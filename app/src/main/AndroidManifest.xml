<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.jackkell.mimicryproject" >

    <!-- Permission - Internet Connect -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".testactivities.HeyWorldTestActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".testactivities.GetTimelineActivity"
            android:label="@string/title_activity_get_timeline" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mimicryactivies.LoginActivity"
            android:label="@string/title_activity_login" >
        </activity>
        <activity
            android:name=".mimicryactivies.ImpersonatorViewActivity"
            android:label="@string/title_activity_impersonator_view" >
        </activity>
        <activity
            android:name=".mimicryactivies.ImpersonatorCreationActivity"
            android:label="@string/title_activity_impersonator_creation" >
        </activity>
        <activity
            android:name=".mimicryactivies.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
        <activity
            android:name=".mimicryactivies.InfomationActivity"
            android:label="@string/title_activity_infomation" >
        </activity>
    </application>

</manifest>
